# 需要提取文案的源文件（相对仓库根目录）。
#
# 说明：xgettext 不认识 .rs 扩展名，会按 C 语法解析（因此对源码里的
# 字符字面量会报「未结束的字符常量」警告，可忽略）。它能正确提取
# t("...") / tf("...") 两种调用，这也是唯一需要关心的。
#
# 另有 N_("...") 标记宏：文案先存进数组、之后才经变量传给 t() 时，
# xgettext 看不见，需要 N_() 包一层。提取时必须带 --keyword=N_
# （见 README 的重新提取命令）。
#
# i18n.rs 不必列入：其中出现的 t()/tf() 全是文档注释与单元测试。
src/main.rs
src/settings.rs
src/intro.rs
src/help.rs

